% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tune_lambda.R
\name{tune_lambda}
\alias{tune_lambda}
\title{Tune weight ridge penalty lambda by pre-period cross-validation}
\usage{
tune_lambda(
  Y_list,
  treat_time,
  treated_units,
  L,
  K,
  lambda_grid = 10^seq(-8, -2, by = 1),
  holdout = 5,
  method = c("blocked", "random"),
  screen_outcome = 1,
  screen_method = c("cor", "mse"),
  max_donors = 1000,
  standardize_outcomes = FALSE,
  eps_sd = 1e-08,
  outcome_weights = NULL,
  time_weights = NULL,
  intercept = c("none", "global", "outcome"),
  solver = c("fw"),
  max_iter = 800,
  tol = 1e-05,
  seed = 1,
  verbose = FALSE,
  parallel_backend = c("none", "fork", "psock"),
  n_cores = 2L,
  unit_ids = NULL,
  tune_units = 200
)
}
\arguments{
\item{Y_list}{List of outcome matrices, each \code{N x T}.}

\item{treat_time}{Numeric vector length \code{N}: first treated period (or \code{Inf}).}

\item{treated_units}{Integer vector of treated unit indices (or IDs if \code{unit_ids} provided).}

\item{L}{Integer. Total number of pre-treatment periods in the design.}

\item{K}{Integer. Post window size used only for donor eligibility in screening.}

\item{lambda_grid}{Numeric vector of candidate \code{lambda} values (>=0).}

\item{holdout}{Integer. Number of pre periods to hold out (must be < \code{L}).}

\item{method}{Character. \code{"blocked"} holds out last \code{holdout} pre periods;
\code{"random"} holds out \code{holdout} randomly (per unit).}

\item{screen_outcome}{Integer. Outcome index used for donor screening.}

\item{screen_method}{Character. \code{"cor"} or \code{"mse"}.}

\item{max_donors}{Integer. Maximum donors after screening.}

\item{standardize_outcomes}{Logical. Passed to \code{build_Xy_for_unit()}.}

\item{eps_sd}{Numeric. Passed to \code{build_Xy_for_unit()} to stabilize standardization.}

\item{outcome_weights}{Optional numeric length \code{M}. Outcome weights for objective.}

\item{time_weights}{Optional numeric length \code{L}. Time weights for objective.}

\item{intercept}{Character. One of \code{"none","global","outcome"} intercept handling.}

\item{solver}{Character. Currently \code{"fw"}.}

\item{max_iter}{Integer. Max iterations for solver inside tuning (keep lower than main fit).}

\item{tol}{Numeric. Tolerance for solver.}

\item{seed}{Optional integer for reproducibility when \code{method="random"}.}

\item{verbose}{Logical. Print progress.}

\item{parallel_backend}{One of \code{"none","fork","psock"}.}

\item{n_cores}{Integer. Number of workers/cores.}

\item{unit_ids}{Optional vector length \code{N} of unit IDs for mapping.}

\item{tune_units}{Integer. If provided, sample this many treated units for tuning.}
}
\value{
A list with:
\describe{
\item{lambda_opt}{Chosen lambda minimizing mean validation MSE across tuned units.}
\item{cv}{Data.frame with columns \code{lambda}, \code{mse_mean}, \code{mse_median}, \code{n_units}.}
\item{lambda_grid}{The searched grid.}
\item{holdout}{Holdout size.}
\item{method}{CV method.}
\item{units_used}{Treated units used for tuning (indices).}
}
}
\description{
Selects \code{lambda} for the donor weight solver by minimizing pre-treatment
validation error on held-out pre periods. This tunes the ridge penalty in the
simplex weight problem:
\deqn{\min_{w\in\Delta}\ \|y - Xw\|_W^2 + \lambda \|w\|_2^2}
where \code{y, X} are stacked multi-outcome pre-treatment data (after any
intercept projection and row-weight pre-whitening).
}
